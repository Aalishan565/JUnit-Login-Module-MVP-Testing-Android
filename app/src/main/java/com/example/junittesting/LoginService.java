package com.example.junittesting;

public class LoginService {
    public boolean login(String username, String password) {
        return "Aalishan".equals(username) && "Ansari".equals(password);
    }
}
